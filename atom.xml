<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://SandMonth.github.io</id>
    <title>SandMonth</title>
    <updated>2021-07-11T15:49:57.982Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://SandMonth.github.io"/>
    <link rel="self" href="https://SandMonth.github.io/atom.xml"/>
    <subtitle>个人记录博客</subtitle>
    <logo>https://SandMonth.github.io/images/avatar.png</logo>
    <icon>https://SandMonth.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, SandMonth</rights>
    <entry>
        <title type="html"><![CDATA[[树链剖分,LCA,换根]CF916E Jamie and Tree]]></title>
        <id>https://SandMonth.github.io/post/cf916e/</id>
        <link href="https://SandMonth.github.io/post/cf916e/">
        </link>
        <updated>2021-07-11T15:39:30.000Z</updated>
        <summary type="html"><![CDATA[<p>[树链剖分,LCA,换根]CF916E Jamie and Tree</p>
]]></summary>
        <content type="html"><![CDATA[<p>[树链剖分,LCA,换根]CF916E Jamie and Tree</p>
<!-- more -->
<blockquote>
<p>前置知识： 线段树 树链剖分 LCA<br>
题目： CF916E Jamie and Tree</p>
</blockquote>
<h3 id="题意">题意：</h3>
<p>有一棵n个节点的有根树，标号为1-n，你需要维护以下三种操作</p>
<ol>
<li>
<p>给定一个点v，将整颗树的根变为v</p>
</li>
<li>
<p>给定两个点u, v，寻找最小子树包含u,v的点并把该树的子树加上x</p>
</li>
<li>
<p>给定一个点v，你需要回答以v所在的子树的权值和</p>
</li>
</ol>
<h3 id="题解">题解:</h3>
<p>分析题目，如果将第一个操作去除 那么其实就是树链剖分的裸题，对于第二个操作可以直接使用LCA来找出该点，因为只需要找到两个点中的最近公共祖先即可。</p>
<p>那么加上第一个操作,也就是将整颗树换根，暴力的想每次换根重新计算一次dfs不太现实,那么可以在初始时将1做为树的根结点之后换根时以一个root变量来记录,之后的操作以root和原始根1来进行计算。</p>
<p>我们先讨论加上换根之后第3个操作如何实现。</p>
<p>首先分类讨论</p>
<ol>
<li>如果要找的点v刚好是root则直接输出整颗树的和</li>
<li>如果v是root的子树中的一个值，则直接找v的子树即可， 因为不敢如何根的子树范围是不会变的如图</li>
</ol>
<figure data-type="image" tabindex="1"><img src="https://SandMonth.github.io/post-images/1626018113521.png" alt="" loading="lazy"></figure>
<ol start="3">
<li>如果是在root子树之上则 需要找到对于v点所在的一个子结点包括root点的点，有点抽象看图吧<br>
<img src="https://SandMonth.github.io/post-images/1626018120856.png" alt="" loading="lazy"><br>
以上就是换根后的需要注意的点</li>
</ol>
<pre><code class="language-cpp">ll lca(ll x, ll y){
    while(top[x] != top[y]){
        if(deep[top[x]] &lt; deep[top[y]]) swap(x, y);
        if(fas[top[x]] == y) return top[x];
        x = fas[top[x]];
    }
    if(dfn[x] &gt; dfn[y]) swap(x, y);
    return wson[x];
}
ll querys(ll x){
    if(x == root) return tree[1];
    if(dfn[x] &lt;= dfn[root] &amp;&amp; dfn[root] &lt;= dfn[x] + sz[x] - 1){
        ll v = lca(x, root);
            ll ans = query(1, 1, n, 1, n);
            ans -= query(1, 1, n, dfn[v], dfn[v] + sz[v] - 1);
        return ans;
    }
    return query(1, 1, n, dfn[x], dfn[x] + sz[x] - 1);
}
</code></pre>
<p>之后我们再讨论 对于u和v的修改操作</p>
<p>还是分类讨论</p>
<ol>
<li>如果x和y在root子树，则点LCA(x, y) 就是换根之后需要修改的值</li>
<li>x和y其中一个在root的子树中，那么要修改的子树点必定是root<br>
<img src="https://SandMonth.github.io/post-images/1626018148274.png" alt="" loading="lazy"></li>
<li>x和y不在root子树中，那么需要x和y 与root之间的关系,也就是它们到达的最低点p，u，分别代表x和root之间的lca和y和root之间的lca 中层次最大的点<br>
<img src="https://SandMonth.github.io/post-images/1626018153237.png" alt="" loading="lazy"></li>
</ol>
<pre><code class="language-cpp">ll LCA(ll x, ll y ){
    while(top[x] != top[y]){
        if(deep[top[x]] &lt; deep[top[y]]) swap(x, y);
        x = fas[top[x]];
    }
    if(deep[x] &gt; deep[y]) swap(x, y);
    return x;
}
void up_tree(ll x, ll y, ll v){
    ll as = LCA(x, root), bs = LCA(y, root), cs = LCA(x, y);
    ll vs = as;
    if(deep[vs] &lt;= deep[bs]) vs = bs;
    if(deep[vs] &lt;= deep[cs]) vs = cs;
    if(vs == root) {
        update(1, 1, n, 1, n, v);
        return;
    }
    if(dfn[vs] &lt;= dfn[root] &amp;&amp; dfn[root] &lt;= dfn[vs] + sz[vs] - 1){
        ll pp = lca(vs, root);
        update(1, 1, n, 1, n, v);
        vs = pp;
        update(1, 1, n, dfn[vs], dfn[vs] + sz[vs] - 1, -v);
        return;
    }
    update(1, 1, n, dfn[vs], dfn[vs] + sz[vs] - 1, v);
    return;
}
</code></pre>
<blockquote>
<p>复杂度: O(nmlogn)</p>
</blockquote>
<h3 id="完整代码">完整代码</h3>
<pre><code class="language-cpp">const long long inf=1e18;
const int N= 7e5 + 10;
const int M= 2e5 + 10;
const ll mod=1e9+7;
using namespace std;
ll n, q;
ll a[N];
struct Node{
    ll to, nxt;
}e[N];
ll head[N], tot = 0;
void add(ll a, ll b){
    e[tot].to = b;
    e[tot].nxt = head[a];
    head[a] = tot ++;
}
ll wson[N], sz[N], dfn[N], deep[N], fas[N], pre[N], top[N];
void dfs1(ll to, ll fa){
    sz[to] = 1;
    deep[to] = deep[fa] + 1;
    fas[to] = fa;
    for(ll i = head[to]; i + 1; i = e[i].nxt){
        ll y = e[i].to;
        if(y == fa) continue;
        dfs1(y, to);
        sz[to] += sz[y];

        if(sz[wson[to]] &lt; sz[y]) wson[to] = y;
    }
}
ll cnt =0 ;
void dfs2(ll to, ll de){
    dfn[to] = ++ cnt;
    pre[cnt] = to;
    top[to] = de;
    if(wson[to]) dfs2(wson[to], de);
    else return ;
    for(ll i = head[to]; i + 1; i = e[i].nxt){
        ll y = e[i].to;
        if(y == fas[to] || y == wson[to]) continue;
        dfs2(y, y);
    }
}
ll tree[N], lazy[N];
void build(ll rt, ll l, ll r){
    if(l == r) {
        tree[rt] += a[pre[l]];
        return;
    }
    ll mid = (l + r) &gt;&gt; 1;
    build(rt &lt;&lt; 1, l, mid);
    build(rt &lt;&lt; 1 | 1, mid + 1, r);
    tree[rt] = tree[rt &lt;&lt; 1] + tree[rt &lt;&lt; 1 | 1];
}
void push_lazy(ll rt, ll l, ll r){
    if(lazy[rt]){
        lazy[rt &lt;&lt; 1] += lazy[rt];
        lazy[rt &lt;&lt; 1 | 1] += lazy[rt];
        ll mid = (l + r) &gt;&gt; 1;
        ll ls = l, rs = mid;
        tree[rt &lt;&lt; 1] += (rs - ls + 1) * lazy[rt];
        ls = mid + 1; rs = r;
        tree[rt &lt;&lt; 1 | 1] += (rs - ls + 1) * lazy[rt];
        lazy[rt] = 0;
    }
}
void update(ll rt, ll l, ll r, ll L, ll R, ll v){
    if(L &lt;= l &amp;&amp; r &lt;= R){
        tree[rt] += (r - l + 1) * v;
        lazy[rt] += v;
        return;
    }
    ll mid = (l + r) &gt;&gt; 1;
    push_lazy(rt, l, r);
    if(L &lt;= mid) update(rt &lt;&lt; 1, l, mid, L, R, v);
    if(mid &lt; R) update(rt &lt;&lt; 1 | 1, mid + 1, r, L, R, v);
    push_lazy(rt, l, r);
    tree[rt] = tree[rt &lt;&lt; 1] + tree[rt &lt;&lt; 1| 1];
}
ll root = 1;
ll LCA(ll x, ll y ){
    while(top[x] != top[y]){
        if(deep[top[x]] &lt; deep[top[y]]) swap(x, y);
        x = fas[top[x]];
    }
    if(deep[x] &gt; deep[y]) swap(x, y);
    return x;
}
ll lca(ll x, ll y){
    while(top[x] != top[y]){
        if(deep[top[x]] &lt; deep[top[y]]) swap(x, y);
        if(fas[top[x]] == y) return top[x];
        x = fas[top[x]];
    }
    if(dfn[x] &gt; dfn[y]) swap(x, y);
    return wson[x];
}
ll get(ll x){
    ll v = root;
    while(top[v] != top[x]){
        if(fas[top[v]] == x) return top[v];
        v = fas[top[v]];
    }
    return wson[x];
}
void up_tree(ll x, ll y, ll v){
    ll as = LCA(x, root), bs = LCA(y, root), cs = LCA(x, y);
    ll vs = as;
    if(deep[vs] &lt;= deep[bs]) vs = bs;
    if(deep[vs] &lt;= deep[cs]) vs = cs;
    if(vs == root) {
        update(1, 1, n, 1, n, v);
        return;
    }
    if(dfn[vs] &lt;= dfn[root] &amp;&amp; dfn[root] &lt;= dfn[vs] + sz[vs] - 1){
        ll pp = lca(vs, root);
        update(1, 1, n, 1, n, v);
        vs = pp;
        update(1, 1, n, dfn[vs], dfn[vs] + sz[vs] - 1, -v);
        return;
    }
    update(1, 1, n, dfn[vs], dfn[vs] + sz[vs] - 1, v);
    return;
}
ll query(ll rt, ll l, ll r, ll L, ll R){
    if(L &lt;= l &amp;&amp; r &lt;= R) return tree[rt];
    ll mid = (l + r) &gt;&gt; 1;
    push_lazy(rt, l, r);
    ll ans = 0;
    if(L &lt;= mid) ans += query(rt &lt;&lt; 1, l, mid, L, R);
    if(mid &lt; R) ans += query(rt &lt;&lt; 1 | 1, mid + 1, r, L, R);
    push_lazy(rt, l, r);
    tree[rt] =  tree[rt &lt;&lt; 1 | 1] + tree[rt &lt;&lt; 1];
    return ans;
}
ll querys(ll x){
    if(x == root) return tree[1];
    if(dfn[x] &lt;= dfn[root] &amp;&amp; dfn[root] &lt;= dfn[x] + sz[x] - 1){
        ll v = lca(x, root);
            ll ans = query(1, 1, n, 1, n);
            ans -= query(1, 1, n, dfn[v], dfn[v] + sz[v] - 1);
        return ans;
    }
    return query(1, 1, n, dfn[x], dfn[x] + sz[x] - 1);
}
int main(){
    RLL2(n, q);
    MM(head, -1);
    FOR(i, 1, n) RLL(a[i]);
    FOR(i, 1, n - 1) {
        ll l, r;
        RLL2(l, r);
        add(l, r);add(r, l);
    }
    dfs1(1, 0);
    dfs2(1, 1);
    build(1, 1, n);
    while(q --){
        ll k; RLL(k);
        if(k == 1){
            RLL(root);
        }else if(k == 2){
            ll l, r, v;
            RLL3(l, r, v);
            up_tree(l, r, v);
        }else{
            ll v; RLL(v);
            PLN(querys(v));
        }
    }
    return 0;
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ [落谷-紫题-线段树-树链剖分] P6157-有趣的游戏]]></title>
        <id>https://SandMonth.github.io/post/P6157youqudeyouxi/</id>
        <link href="https://SandMonth.github.io/post/P6157youqudeyouxi/">
        </link>
        <updated>2021-07-01T14:36:24.000Z</updated>
        <summary type="html"><![CDATA[<p>[落谷-紫题-线段树-树链剖分] P6157-有趣的游戏</p>
]]></summary>
        <content type="html"><![CDATA[<p>[落谷-紫题-线段树-树链剖分] P6157-有趣的游戏</p>
<h2 id="-more-"><!-- more --></h2>
<blockquote>
<p>前置知识: 树链剖分 线段树 LCA<br>
题目：落谷 P6157<br>
ps:头次做出紫题纪念下,滑稽～滑稽～</p>
</blockquote>
<hr>
<h2 id="题意">题意:</h2>
<p>​	小A和小B在树上玩游戏，其中树上的每个点都有点权,第i个点的点权为<span class="katex"><span class="katex-mathml"><math><semantics><mrow><msub><mi>w</mi><mi>i</mi></msub></mrow><annotation encoding="application/x-tex">w_i</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.58056em;vertical-align:-0.15em;"></span><span class="mord"><span class="mord mathdefault" style="margin-right:0.02691em;">w</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.31166399999999994em;"><span style="top:-2.5500000000000003em;margin-left:-0.02691em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathdefault mtight">i</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.15em;"><span></span></span></span></span></span></span></span></span></span>,每次系统会给出一条链,小A可以选出这条链上不同的两个点权x,y，这样小A的得分为<span class="katex"><span class="katex-mathml"><math><semantics><mrow><msub><mi>w</mi><mi>x</mi></msub><mi>m</mi><mi>o</mi><mi>d</mi><msub><mi>w</mi><mi>y</mi></msub></mrow><annotation encoding="application/x-tex">w_x mod w_y</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.980548em;vertical-align:-0.286108em;"></span><span class="mord"><span class="mord mathdefault" style="margin-right:0.02691em;">w</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.151392em;"><span style="top:-2.5500000000000003em;margin-left:-0.02691em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathdefault mtight">x</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.15em;"><span></span></span></span></span></span></span><span class="mord mathdefault">m</span><span class="mord mathdefault">o</span><span class="mord mathdefault">d</span><span class="mord"><span class="mord mathdefault" style="margin-right:0.02691em;">w</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.15139200000000003em;"><span style="top:-2.5500000000000003em;margin-left:-0.02691em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathdefault mtight" style="margin-right:0.03588em;">y</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.286108em;"><span></span></span></span></span></span></span></span></span></span>, 选完之后小B从整颗树上进行选择,计分与小A同理.</p>
<p>​	系统每次会给出两个操作,一为树上的一个点添加<span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>v</mi></mrow><annotation encoding="application/x-tex">v</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.43056em;vertical-align:0em;"></span><span class="mord mathdefault" style="margin-right:0.03588em;">v</span></span></span></span>值,一给出一条链给你求出小A的最大值和小B的最大值.</p>
<p>​	如果小A无法选出两个不同的值则只能输出-1</p>
<h2 id="题解">题解:</h2>
<p>​	先分析题目,首先题目说明是在树上的链进行操作(明示了),并且一个操作是给出一个条树上的链求值,那么我考虑使用树链剖分,对于修改点值可以使用树链剖分的线段树修改。</p>
<p>树链剖分初始化模版冲：</p>
<pre><code class="language-cpp">ll deep[N], wson[N], top[N], dfn[N], fas[N], pre[N], sz[N];
// 树深度 重儿子 链头 dfs序 父亲结点 前序序列 子树大小
void dfs1(ll to, ll fa){
    fas[to] = fa;
    deep[to] = deep[fa] + 1;
    sz[to] = 1;
    for(ll i = head[to]; i + 1; i = e[i].nxt){
        ll y = e[i].to;
        if(y == fa) continue;
        dfs1(y, to);
        sz[to] += sz[y];
        if(sz[wson[to]] &lt; sz[y]){
            wson[to] = y;
        }
    }

}
ll cnt = 0;
void dfs2(ll to, ll de){
    dfn[to] = ++ cnt;
    pre[cnt] = to;
    top[to] = de;
    if(wson[to]){
        dfs2(wson[to], de);
    }else return;
    for(ll i = head[to]; i + 1; i = e[i].nxt){
        ll y = e[i].to;
        if(y == wson[to] || y == fas[to]) continue;
        dfs2(y, y);
    }
}
</code></pre>
<p>​	确定了使用树链剖分之后,在观察题目考虑树上维护什么值,首先分析小A<span class="katex"><span class="katex-mathml"><math><semantics><mrow><msub><mi>w</mi><mi>x</mi></msub><mi>m</mi><mi>o</mi><mi>d</mi><msub><mi>w</mi><mi>y</mi></msub></mrow><annotation encoding="application/x-tex">w_x mod w_y</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.980548em;vertical-align:-0.286108em;"></span><span class="mord"><span class="mord mathdefault" style="margin-right:0.02691em;">w</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.151392em;"><span style="top:-2.5500000000000003em;margin-left:-0.02691em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathdefault mtight">x</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.15em;"><span></span></span></span></span></span></span><span class="mord mathdefault">m</span><span class="mord mathdefault">o</span><span class="mord mathdefault">d</span><span class="mord"><span class="mord mathdefault" style="margin-right:0.02691em;">w</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.15139200000000003em;"><span style="top:-2.5500000000000003em;margin-left:-0.02691em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathdefault mtight" style="margin-right:0.03588em;">y</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.286108em;"><span></span></span></span></span></span></span></span></span></span>要最大,那么我们贪心的想当<span class="katex"><span class="katex-mathml"><math><semantics><mrow><msub><mi>w</mi><mi>x</mi></msub></mrow><annotation encoding="application/x-tex">w_x</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.58056em;vertical-align:-0.15em;"></span><span class="mord"><span class="mord mathdefault" style="margin-right:0.02691em;">w</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.151392em;"><span style="top:-2.5500000000000003em;margin-left:-0.02691em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathdefault mtight">x</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.15em;"><span></span></span></span></span></span></span></span></span></span>为次大并且<span class="katex"><span class="katex-mathml"><math><semantics><mrow><msub><mi>w</mi><mi>y</mi></msub></mrow><annotation encoding="application/x-tex">w_y</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.716668em;vertical-align:-0.286108em;"></span><span class="mord"><span class="mord mathdefault" style="margin-right:0.02691em;">w</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.15139200000000003em;"><span style="top:-2.5500000000000003em;margin-left:-0.02691em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathdefault mtight" style="margin-right:0.03588em;">y</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.286108em;"><span></span></span></span></span></span></span></span></span></span>为最大的时候该值会最大,大家可以敢情理解一下.既然是次大和最大那么对于这颗树可以使用两个值对其进行最大和次大的维护.</p>
<p>​	对于最开始的建树[其实可以不用pair因为作者最开始做的时候想了一个时间度高的做法,虽然开o2优化过了]：</p>
<pre><code class="language-cpp">void push_tree(ll rt){
    pair&lt;ll,ll&gt; a1 = tree[rt &lt;&lt; 1];
    pair&lt;ll,ll&gt; a2 = tree[rt &lt;&lt; 1 | 1];
    pair&lt;ll,ll&gt; a3 = tree2[rt &lt;&lt; 1];
    pair&lt;ll,ll&gt; a4 = tree2[rt &lt;&lt; 1 | 1];
    ll mx = max(a1.first,max(a2.first, max(a3.first,a4.first)));
    tree[rt].first = mx;
    ll mx2 = max(a1.first == mx ? -1 : a1.first,max(a2.first == mx ? -1 : a2.first, max(a3.first == mx ? -1 : a3.first,a4.first == mx ? -1 : a4.first)));
    tree2[rt].first = mx2;
}
void build(ll rt, ll l, ll r){ //线段树建树
    ls[rt] = l; rs[rt] = r;
    if(l == r){
        tree[rt] = make_pair(a[pre[l]], l);
        tree2[rt] = make_pair(a[pre[l]], l);
        return ;
    }
    ll mid = (l + r) &gt;&gt; 1;
    build(rt &lt;&lt; 1, l, mid);
    build(rt &lt;&lt; 1 | 1, mid + 1, r);
    push_tree(rt);
}
</code></pre>
<p>​	然后使用LCA对小A的最大值进行计算,以下的mx和cmx最后的结果就是最大值和次大值.</p>
<pre><code class="language-cpp">    pair&lt;ll, ll&gt; mx = make_pair(-1, -1), cmx = make_pair(-1, -1);
    while(top[x] != top[y]){
        if(dfn[top[x]] &lt; dfn[top[y]]) swap(x, y);
        query(1, 1, n, dfn[top[x]], dfn[x], mx, cmx);
        x = fas[top[x]];
    }
    if(dfn[x] &gt; dfn[y]) swap(x, y);
    query(1, 1, n, dfn[x], dfn[y], mx, cmx);
</code></pre>
<pre><code class="language-cpp">//查找
void query(ll rt, ll l, ll r, ll L, ll R, pair&lt;ll,ll&gt; &amp;a, pair&lt;ll,ll&gt; &amp;b){
    if(L &lt;= l &amp;&amp; r &lt;= R){
        pair&lt;ll, ll&gt; pp[10];
        pp[1] = a; pp[2] = b; pp[3] = tree[rt]; pp[4] = tree2[rt];
        sort(pp + 1, pp + 1 + 4);
        a = pp[4];
        for(ll i = 3; i &gt;= 1; i --){
            if(pp[i].first != pp[4].first){
                b = pp[i];
                return;
            }
        }
        b = pp[4];
        return;
    }
    push_tree(rt);
    ll mid = (l + r) &gt;&gt; 1;
    if(L &lt;= mid) query(rt &lt;&lt; 1, l, mid, L, R, a, b);
    if(mid &lt; R) query(rt &lt;&lt; 1 | 1, mid + 1, r, L, R, a, b);
}
</code></pre>
<p>​</p>
<p>​	对于小B的操作,大伙最开始的想法肯定是对其树进行点对修改,将点的值该为最小之后取线段树的头结点即可[我最开始也是这么想的],但这样的复杂度最坏为O(deep[tree])而且每次要进行4次操作如果树大的话铁打的超时(如果这么做了可以开个O2优化过优化后巨快).</p>
<p>​	因为小B是对于整颗树取最大和次大,其实就是维护整个树的值的最大和次大值(其中去除已选的),考虑使用multiset来维护,每次将已选的最大和次大在此数组中删除取出set的次大作为答案,然后再插入回去.每次删除和插入的复杂度为O(logn)并且每次删除需要使用find 找到值所以实际复杂度为O(2logn).</p>
<pre><code class="language-cpp">ll s1 = mx.first, s2 = cmx.first;
st.erase(st.find(s1));
st.erase(st.find(s2));
ll ans = *(--st.lower_bound(*--st.end()));
st.insert(s1);
st.insert(s2);
</code></pre>
<p>​	对于每次修改一个点的操作使用线段树的区间操作</p>
<pre><code class="language-cpp">void update(ll rt, ll l, ll r, ll L, ll R, ll v){
    if(L &lt;= l &amp;&amp; r &lt;= R){
        ll val = tree[rt].first;
        st.erase(st.find(val));
        st.insert(val + v);
        tree[rt] = make_pair(tree[rt].first + v, l);
        tree2[rt] = make_pair(tree2[rt].first + v, l);
        return;
    }
    ll mid = (l + r) &gt;&gt; 1;
    if(L &lt;= mid) update(rt &lt;&lt; 1, l, mid, L, R, v);
    else update(rt &lt;&lt; 1 | 1, mid + 1, r, L, R, v);
    push_tree(rt);
}
</code></pre>
<blockquote>
<p>对于整个算法复杂度为O(6lognmlog^n)</p>
</blockquote>
<h2 id="完整代码">完整代码</h2>
<blockquote>
<p>该代码为无须开O2度版本</p>
</blockquote>
<pre><code class="language-cpp">struct Node{
    ll to, nxt;
}e[N];
multiset&lt;ll&gt; st;
ll tot = 0, head[N];
void add(ll a, ll b){
    e[tot].to = b;
    e[tot].nxt = head[a];
    head[a] = tot ++;
}
ll n;
ll a[N];
ll deep[N], wson[N], top[N], dfn[N], fas[N], pre[N], sz[N];
void dfs1(ll to, ll fa){
    fas[to] = fa;
    deep[to] = deep[fa] + 1;
    sz[to] = 1;
    for(ll i = head[to]; i + 1; i = e[i].nxt){
        ll y = e[i].to;
        if(y == fa) continue;
        dfs1(y, to);
        sz[to] += sz[y];
        if(sz[wson[to]] &lt; sz[y]){
            wson[to] = y;
        }
    }

}
ll cnt = 0;
void dfs2(ll to, ll de){
    dfn[to] = ++ cnt;
    pre[cnt] = to;
    top[to] = de;
    if(wson[to]){
        dfs2(wson[to], de);
    }else return;
    for(ll i = head[to]; i + 1; i = e[i].nxt){
        ll y = e[i].to;
        if(y == wson[to] || y == fas[to]) continue;
        dfs2(y, y);
    }
}
ll ls[N], rs[N];
pair&lt;ll,ll&gt; tree[N], tree2[N];
void push_tree(ll rt){
    pair&lt;ll,ll&gt; a1 = tree[rt &lt;&lt; 1];
    pair&lt;ll,ll&gt; a2 = tree[rt &lt;&lt; 1 | 1];
    pair&lt;ll,ll&gt; a3 = tree2[rt &lt;&lt; 1];
    pair&lt;ll,ll&gt; a4 = tree2[rt &lt;&lt; 1 | 1];
    ll mx = max(a1.first,max(a2.first, max(a3.first,a4.first)));
    tree[rt].first = mx;
    ll mx2 = max(a1.first == mx ? -1 : a1.first,max(a2.first == mx ? -1 : a2.first, max(a3.first == mx ? -1 : a3.first,a4.first == mx ? -1 : a4.first)));
    tree2[rt].first = mx2;
}
void build(ll rt, ll l, ll r){
    ls[rt] = l; rs[rt] = r;
    if(l == r){
        tree[rt] = make_pair(a[pre[l]], l);
        tree2[rt] = make_pair(a[pre[l]], l);
        return ;
    }
    ll mid = (l + r) &gt;&gt; 1;
    build(rt &lt;&lt; 1, l, mid);
    build(rt &lt;&lt; 1 | 1, mid + 1, r);
    push_tree(rt);
}
void update(ll rt, ll l, ll r, ll L, ll R, ll v){
    if(L &lt;= l &amp;&amp; r &lt;= R){
        ll val = tree[rt].first;
        st.erase(st.find(val));
        st.insert(val + v);
        tree[rt] = make_pair(tree[rt].first + v, l);
        tree2[rt] = make_pair(tree2[rt].first + v, l);
        return;
    }
    ll mid = (l + r) &gt;&gt; 1;
    if(L &lt;= mid) update(rt &lt;&lt; 1, l, mid, L, R, v);
    else update(rt &lt;&lt; 1 | 1, mid + 1, r, L, R, v);
    push_tree(rt);
}

void query(ll rt, ll l, ll r, ll L, ll R, pair&lt;ll,ll&gt; &amp;a, pair&lt;ll,ll&gt; &amp;b){
    if(L &lt;= l &amp;&amp; r &lt;= R){
        pair&lt;ll, ll&gt; pp[10];
        pp[1] = a; pp[2] = b; pp[3] = tree[rt]; pp[4] = tree2[rt];
        sort(pp + 1, pp + 1 + 4);
        a = pp[4];
        for(ll i = 3; i &gt;= 1; i --){
            if(pp[i].first != pp[4].first){
                b = pp[i];
                return;
            }
        }
        b = pp[4];
        return;
    }
    push_tree(rt);
    ll mid = (l + r) &gt;&gt; 1;
    if(L &lt;= mid) query(rt &lt;&lt; 1, l, mid, L, R, a, b);
    if(mid &lt; R) query(rt &lt;&lt; 1 | 1, mid + 1, r, L, R, a, b);
}

void slove(ll x, ll y){
    pair&lt;ll, ll&gt; mx = make_pair(-1, -1), cmx = make_pair(-1, -1);
    while(top[x] != top[y]){
        if(dfn[top[x]] &lt; dfn[top[y]]) swap(x, y);
        query(1, 1, n, dfn[top[x]], dfn[x], mx, cmx);
        x = fas[top[x]];
    }
    if(dfn[x] &gt; dfn[y]) swap(x, y);
    query(1, 1, n, dfn[x], dfn[y], mx, cmx);
    if(mx.first == -1 || cmx.first == -1 || mx.first == cmx.first){
        PLN((ll) -1);
        return ;
    }
    ll s1 = mx.first, s2 = cmx.first;
    st.erase(st.find(s1));
    st.erase(st.find(s2));
    ll ans = *(--st.lower_bound(*--st.end()));
    st.insert(s1);
    st.insert(s2);
    PLN2(cmx.first, ans);
}
int main(){
    MM(head, -1);
    RLL(n);
    FOR(i, 1, n - 1){
        ll l, r; RLL2(l, r);
        add(l, r); add(r, l);
    }
    FOR(i, 1, n) RLL(a[i]), st.insert(a[i]);
    dfs1(1, 0);
    dfs2(1, 1);
    build(1, 1, n);
    ll q;
    RLL(q);
    while (q --){
        ll k; RLL(k);
        if(k == 0){
            ll x, v;
            RLL2(x, v);
            x = dfn[x];
            update(1, 1, n, x, x, v);
        }else{
            ll x, y; RLL2(x, y);
            slove(x, y);
        }
    }
    return 0;
}
</code></pre>
<blockquote>
<p>该版本为开O2能过的有点暴力的做法(最开始过的代码后面看了一眼multiset才想到这么优化[太菜了])</p>
</blockquote>
<pre><code class="language-cpp">struct Node{
    ll to, nxt;
}e[N];
ll tot = 0, head[N];
void add(ll a, ll b){
    e[tot].to = b;
    e[tot].nxt = head[a];
    head[a] = tot ++;
}
ll n;
ll a[N];
ll deep[N], wson[N], top[N], dfn[N], fas[N], pre[N], sz[N];
void dfs1(ll to, ll fa){
    fas[to] = fa;
    deep[to] = deep[fa] + 1;
    sz[to] = 1;
    for(ll i = head[to]; i + 1; i = e[i].nxt){
        ll y = e[i].to;
        if(y == fa) continue;
        dfs1(y, to);
        sz[to] += sz[y];
        if(sz[wson[to]] &lt; sz[y]){
            wson[to] = y;
        }
    }

}
ll cnt = 0;
void dfs2(ll to, ll de){
    dfn[to] = ++ cnt;
    pre[cnt] = to;
    top[to] = de;
    if(wson[to]){
        dfs2(wson[to], de);
    }else return;
    for(ll i = head[to]; i + 1; i = e[i].nxt){
        ll y = e[i].to;
        if(y == wson[to] || y == fas[to]) continue;
        dfs2(y, y);
    }
}
ll ls[N], rs[N];
pair&lt;ll,ll&gt; tree[N], tree2[N];
void push_tree(ll rt){
    pair&lt;ll,ll&gt; as[10];
    as[1] = tree[rt &lt;&lt; 1];
    as[2] = tree[rt &lt;&lt; 1 | 1];
    as[3] = tree2[rt &lt;&lt; 1];
    as[4] = tree2[rt &lt;&lt; 1 | 1];
    sort(as + 1, as + 1 + 4);
    tree[rt] = as[4];
    for(ll i = 3; i &gt;= 1; i --){
        if(as[i].first != as[4].first){
            tree2[rt] = as[i];
            return;
        }
    }
    tree2[rt] = as[4];
}
void build(ll rt, ll l, ll r){
    ls[rt] = l; rs[rt] = r;
    if(l == r){
        tree[rt] = make_pair(a[pre[l]], l);
        tree2[rt] = make_pair(a[pre[l]], l);
        return ;
    }
    ll mid = (l + r) &gt;&gt; 1;
    build(rt &lt;&lt; 1, l, mid);
    build(rt &lt;&lt; 1 | 1, mid + 1, r);
    push_tree(rt);
}
void update(ll rt, ll l, ll r, ll L, ll R, ll v){
    if(L &lt;= l &amp;&amp; r &lt;= R){
        tree[rt] = make_pair(tree[rt].first + v, l);
        tree2[rt] = make_pair(tree2[rt].first + v, l);
        return;
    }
    ll mid = (l + r) &gt;&gt; 1;
    if(L &lt;= mid) update(rt &lt;&lt; 1, l, mid, L, R, v);
    else update(rt &lt;&lt; 1 | 1, mid + 1, r, L, R, v);
    push_tree(rt);
}

void query(ll rt, ll l, ll r, ll L, ll R, pair&lt;ll,ll&gt; &amp;a, pair&lt;ll,ll&gt; &amp;b){
    if(L &lt;= l &amp;&amp; r &lt;= R){
        pair&lt;ll, ll&gt; pp[10];
        pp[1] = a; pp[2] = b; pp[3] = tree[rt]; pp[4] = tree2[rt];
        sort(pp + 1, pp + 1 + 4);
        a = pp[4];
        for(ll i = 3; i &gt;= 1; i --){
            if(pp[i].first != pp[4].first){
                b = pp[i];
                return;
            }
        }
        b = pp[4];
        return;
    }
    push_tree(rt);
    ll mid = (l + r) &gt;&gt; 1;
    if(L &lt;= mid) query(rt &lt;&lt; 1, l, mid, L, R, a, b);
    if(mid &lt; R) query(rt &lt;&lt; 1 | 1, mid + 1, r, L, R, a, b);
}
void slove(ll x, ll y){
    pair&lt;ll, ll&gt; mx = make_pair(-1, -1), cmx = make_pair(-1, -1);
    while(top[x] != top[y]){
        if(dfn[top[x]] &lt; dfn[top[y]]) swap(x, y);
        query(1, 1, n, dfn[top[x]], dfn[x], mx, cmx);
        x = fas[top[x]];
    }
    if(dfn[x] &gt; dfn[y]) swap(x, y);
    query(1, 1, n, dfn[x], dfn[y], mx, cmx);
    if(mx.first == -1 || cmx.first == -1 || mx.first == cmx.first){
        PLN((ll) -1);
        return ;
    }
    ll s1 = mx.first, s2 = cmx.first;
    update(1, 1, n, mx.second, mx.second, -(s1 ));
    update(1, 1, n, cmx.second, cmx.second, -(s2 ));
    pair&lt;ll, ll&gt; mxs = tree[1], cmxs = tree2[1];
    update(1, 1, n, mx.second, mx.second, (s1 ));
    update(1, 1, n, cmx.second, cmx.second, (s2 ));
    PLN2(cmx.first % mx.first, cmxs.first % mxs.first);
}
int main(){
    MM(head, -1);
    RLL(n);
    FOR(i, 1, n - 1){
        ll l, r; RLL2(l, r);
        add(l, r); add(r, l);
    }
    FOR(i, 1, n) RLL(a[i]);
    dfs1(1, 0);
    dfs2(1, 1);
    build(1, 1, n);
    ll q;
    RLL(q);
    while (q --){
        ll k; RLL(k);
        if(k == 0){
            ll x, v;
            RLL2(x, v);
            x = dfn[x];
            update(1, 1, n, x, x, v);
        }else{
            ll x, y; RLL2(x, y);
            slove(x, y);
        }
    }
    return 0;
}

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[轻重链剖分教程(自用) + 模版]]></title>
        <id>https://SandMonth.github.io/post/qzlpf/</id>
        <link href="https://SandMonth.github.io/post/qzlpf/">
        </link>
        <updated>2021-06-20T09:04:45.000Z</updated>
        <summary type="html"><![CDATA[<p>轻重链剖分教程(自用)</p>
]]></summary>
        <content type="html"><![CDATA[<p>轻重链剖分教程(自用)</p>
<!-- more -->
<h1 id="轻重链剖分">轻重链剖分</h1>
<blockquote>
<p>前置知识: DFS序,  线段树,  LCA</p>
</blockquote>
<h2 id="轻重链剖分的作用">轻重链剖分的作用</h2>
<ol>
<li>
<p>将树从x到y结点上的最短路径上的所有值都加上v</p>
</li>
<li>
<p>求树从x到y结点上的最短路径上的所有值的和</p>
</li>
<li>
<p>将以x为根结点的所有结点值加上v</p>
</li>
<li>
<p>求以x为根结点的所有结点值的和</p>
<blockquote>
<p>前面2问中x到y结点上的最短路径很明显就是求x和y的LCA</p>
</blockquote>
</li>
</ol>
<h2 id="算法概念">算法概念</h2>
<p>​	该算法就和它的名字一样就是将一颗树划分成多个链，然后维护这些链. 对于每条链可以看作是一个区间,对于区间操作一般我们选择线段树进行维护(线段树复杂度低,性质多,代码多,线段树nb！)。</p>
<p>​	对于这个算法需要知道一些定义：</p>
<table>
<thead>
<tr>
<th>概念</th>
<th>定义</th>
</tr>
</thead>
<tbody>
<tr>
<td>重儿子[结点]</td>
<td>父亲结点的所有儿子中子树结点数最多的结点</td>
</tr>
<tr>
<td>轻儿子[结点]</td>
<td>父亲结点中除了重儿子以外的所有儿子结点</td>
</tr>
<tr>
<td>重边[一条边]</td>
<td>父亲结点和重儿子连成的一条边</td>
</tr>
<tr>
<td>轻边[一条边]</td>
<td>父亲结点和轻儿子连成的一条边</td>
</tr>
<tr>
<td>重链[多条边]</td>
<td>由多条重边链接而成的路径,链头为轻链</td>
</tr>
<tr>
<td>轻链</td>
<td>由多条轻边链接而成的路径,链头是它自己</td>
</tr>
</tbody>
</table>
<p>​</p>
<p>如下面一张图:<br>
<img src="https://SandMonth.github.io/post-images/1624180242393.png" alt="" loading="lazy"></p>
<ul>
<li>粉色点代表重儿子</li>
<li>黑色点代表轻儿子</li>
<li>相邻红色边连起来代表重边</li>
<li>相邻蓝色边连起来代表轻边</li>
<li>所有红色边代表重链</li>
<li>所有蓝色边代表轻链</li>
</ul>
<blockquote>
<p>对于儿子数相同的两个点来说选谁当重儿子都可以</p>
</blockquote>
<h2 id="算法实现">算法实现</h2>
<p>​	首先需要对一颗树中的所有点进行重新编号, 默认一颗树上的点的编号可能会很乱,为了方便管理需要对这些点进行一个重新编号,但这编号不能乱编</p>
<p>​	对于一颗树的编号顺序:</p>
<pre><code>1. 先遍历重儿子结点进行编号

2. 按照dfs序进行进行编号，在遍历完重儿子后,就遍历其他轻儿子
</code></pre>
<p>使用这种方式遍历编号有几点好处.</p>
<ol>
<li>重链中的所有点一定是连续的,因为每次是优先访问重儿子,所以它们之间的结点必然是连续的</li>
<li>如果(u,v)是一条轻边，那么size[v] &lt;= size[u] / 2,如果大于那么v结点必定是重儿子</li>
<li>每个点到根结点,必定不会超过logn条重路径，和logn条轻路径</li>
</ol>
<p><img src="https://SandMonth.github.io/post-images/1624180272510.png" alt="" loading="lazy"><br>
​	对于一条重链来说我们需要存储链头(即深度最浅的结点),对于一条轻链来说它的链头就是它自己,重链的头结点也算是一个轻儿子.</p>
<p>​	总结元素：</p>
<table>
<thead>
<tr>
<th>定义</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>deep[x]</td>
<td>结点x的深度</td>
</tr>
<tr>
<td>fa[x]</td>
<td>x结点的父亲</td>
</tr>
<tr>
<td>size[x]</td>
<td>以x为根结点的树大小</td>
</tr>
<tr>
<td>wson[x]</td>
<td>结点x的重儿子</td>
</tr>
<tr>
<td>dfn[x]</td>
<td>树的dfs序,为重新编号的序列</td>
</tr>
<tr>
<td>top[x]</td>
<td>x结点的链头</td>
</tr>
<tr>
<td>pre[x]</td>
<td>x结点的前驱结点</td>
</tr>
</tbody>
</table>
<p>​	求出上述结点只需要进行两次dfs操作即可,对于第一次dfs,主要求出每个结点的深度,父亲,树大小,以及重儿子,对于第二dfs根据重儿子求dfs序,并且记录链头以及前驱结点.</p>
<pre><code class="language-cpp">void dfs1(ll to, ll fa){
    sz[to] = 1;
    fas[to] = fa;   //设置父结点
    deep[to] = deep[fa] + 1; //计算深度
    for(ll i = head[to]; i + 1; i = e[i].nxt){
        if(e[i].to == fa) continue;
        dfs1(e[i].to, to);
        sz[to] += sz[e[i].to];  //计算树的大小
        if(sz[wson[to]] &lt; sz[e[i].to]){ //获取重儿子
            wson[to] = e[i].to;
        }
    }
}
ll cnt = 0;
void dfs2(ll to, ll fa){
    dfn[to] = ++ cnt;   //dfs序列(重新编号)
    pre[cnt] = to;  //设置前驱结点
    top[to] = fa;   //设置链头
    if(wson[to]) dfs2(wson[to], fa);    //优先遍历重儿子
    else return;
    for(ll i = head[to]; i + 1; i = e[i].nxt){
        if(wson[to] == e[i].to || e[i].to == fas[to]) continue;
        dfs2(e[i].to, e[i].to); //轻儿子的链头是它自己
    }
}
</code></pre>
<blockquote>
<p>对于两个dfs复杂度为o(2m)</p>
</blockquote>
<p>​	对于求x到y的最短路径通过链头进行LCA操作即可,每次优先修改链头深度最大的点向上跳跃一个区间并通过线段树修改跳跃区间值</p>
<pre><code class="language-cpp">void up_lca_tree(ll x, ll y, ll v){
    while(top[x] != top[y]){
        if(deep[top[x]] &lt; deep[top[y]]) swap(x, y);
        update(1, 1, n, dfn[top[x]], dfn[x], v);
        x = fas[top[x]];
    }
    if(deep[x] &lt; deep[y]) swap(x, y);
    update(1, 1, n, dfn[y], dfn[x], v);
}
</code></pre>
<p>​	如下图找6和9点最短路径,先找6的链头为6(轻儿子的链头是自己)并修改,然后找3的链头为1并修改1-3结点的值(因为重链结点是连续的), 以此类推.最后能维护这段最短距离的区间(绿色指向为x的父亲)<br>
<img src="https://SandMonth.github.io/post-images/1624180299380.png" alt="" loading="lazy"><br>
​	查找同理:</p>
<pre><code class="language-cpp">ll qe_lca_tree(ll x, ll y){
    ll ans = 0;
    while(top[x] != top[y]){
        if(deep[top[x]] &lt; deep[top[y]]) swap(x, y);
        ans += query(1, 1, n, dfn[top[x]], dfn[x]);
        x = fas[top[x]];
    }
    if(deep[x] &lt; deep[y]) swap(x, y);
    ans += query(1, 1, n, dfn[y], dfn[x]);
    return ans;
}
</code></pre>
<blockquote>
<p>显然我们知道线段树的修改和查找复杂度为O(logn),查找树上的距离取决于这条路径上的重链和轻链,由性质可知,重链和轻链的数量不会超过2logn,所以最短距离的修改和查询的总复杂度为O(log^2n)</p>
</blockquote>
<p>​	对于上述的第3，4问修改/查找以x为根结点的所有子结点的值,其实通过上述的编号性质能得出,以x为根结点的所有子树编号范围为:dfn[x] + sz[dfn[x]] - 1,只需要使用线段树进行修改/查询即可</p>
<pre><code class="language-cpp">void tree_up(ll x, ll v){
    update(1, 1, n, dfn[x], dfn[x] + sz[x] - 1, v);
}
ll tree_qe(ll x){
    return query(1 , 1, n, dfn[x], dfn[x] + sz[x] - 1);
}
</code></pre>
<p>树链剖分的总复杂度为O(mlog^n)</p>
<h2 id="模版代码">模版代码</h2>
<blockquote>
<p>题目：【模板】轻重链剖分</p>
<p>链接: https://www.luogu.com.cn/problem/P3384</p>
</blockquote>
<pre><code class="language-cpp">ll n, m, r, p;
ll a[N];
struct Node{
    ll to, nxt;
}e[N];
ll head[N], tot = 0;
void add(ll a, ll b){
    e[tot].to = b;
    e[tot].nxt = head[a];
    head[a] = tot ++ ;
}
ll tree[N], lazy[N];
ll ls[N], rs[N];
ll top[N], pre[N], wson[N], dfn[N], sz[N], deep[N], fas[N];
void build(ll rt, ll l, ll r){
    ls[rt] = l; rs[rt] = r;
    if(l == r){
        tree[rt] = a[pre[l]];
        return;
    }
    ll mid = (l + r) &gt;&gt; 1;
    build(rt &lt;&lt; 1, l, mid);
    build(rt &lt;&lt; 1 | 1, mid + 1, r);
    tree[rt] = tree[rt &lt;&lt; 1] + tree[rt &lt;&lt; 1 | 1];
}
ll vis[N];
void dfs1(ll to, ll fa){
    sz[to] = 1;
    fas[to] = fa;
    deep[to] = deep[fa] + 1;
    for(ll i = head[to]; i + 1; i = e[i].nxt){
        if(e[i].to == fa) continue;
        dfs1(e[i].to, to);
        sz[to] += sz[e[i].to];
        if(sz[wson[to]] &lt; sz[e[i].to]){
            wson[to] = e[i].to;
        }
    }
}
ll cnt = 0;
void dfs2(ll to, ll fa){
    dfn[to] = ++ cnt;
    pre[cnt] = to;
    top[to] = fa;
    if(wson[to]) dfs2(wson[to], fa);
    else return;
    for(ll i = head[to]; i + 1; i = e[i].nxt){
        if(wson[to] == e[i].to || e[i].to == fas[to]) continue;
        dfs2(e[i].to, e[i].to);
    }
}
void push_lazy(ll x){
    if(lazy[x]){
        lazy[x &lt;&lt; 1] += lazy[x];
        lazy[x &lt;&lt; 1 | 1] += lazy[x];
        tree[x &lt;&lt; 1] += lazy[x] * (rs[x &lt;&lt; 1] - ls[x &lt;&lt; 1] + 1);
        tree[x &lt;&lt; 1 | 1] += lazy[x] * (rs[x &lt;&lt; 1 | 1] - ls[x &lt;&lt; 1 | 1] + 1);
        lazy[x] = 0;
    }
}
void update(ll rt, ll l, ll r, ll L, ll R, ll v){
    if(L &lt;= l &amp;&amp; r &lt;= R){
        tree[rt] += v * (r - l + 1);
        lazy[rt] += v;
        tree[rt] %= p;
        return ;
    }
    push_lazy(rt);
    ll mid = (l + r) &gt;&gt; 1;
    if(L &lt;= mid) update(rt &lt;&lt; 1, l, mid, L, R, v);
    if(mid &lt; R) update(rt &lt;&lt; 1 | 1, mid + 1, r, L, R, v);

    tree[rt] = tree[rt &lt;&lt; 1] + tree[rt &lt;&lt; 1 | 1];
}
ll query(ll rt, ll l, ll r, ll L, ll R){
    if(L &lt;= l &amp;&amp; r &lt;= R){
        return tree[rt];
    }
    push_lazy(rt);
    ll ans = 0;
    ll mid = (l + r) &gt;&gt; 1;
    if(L &lt;= mid) ans += query(rt &lt;&lt; 1, l, mid, L, R);
    if(mid &lt; R) ans += query(rt &lt;&lt; 1 | 1, mid + 1, r, L, R);
    tree[rt] = tree[rt &lt;&lt; 1] + tree[rt &lt;&lt; 1 | 1];
    return ans;
}
void up_lca_tree(ll x, ll y, ll v){
    v %= p;
    while(top[x] != top[y]){
        if(deep[top[x]] &lt; deep[top[y]]) swap(x, y);
        update(1, 1, n, dfn[top[x]], dfn[x], v);
        x = fas[top[x]];
    }
    if(deep[x] &lt; deep[y]) swap(x, y);
    update(1, 1, n, dfn[y], dfn[x], v);
}
ll qe_lca_tree(ll x, ll y){
    ll ans = 0;
    while(top[x] != top[y]){
        if(deep[top[x]] &lt; deep[top[y]]) swap(x, y);
        ans += query(1, 1, n, dfn[top[x]], dfn[x]);
        x = fas[top[x]];
    }
    if(deep[x] &lt; deep[y]) swap(x, y);
    ans += query(1, 1, n, dfn[y], dfn[x]);
    return ans;
}
void tree_up(ll x, ll v){
    v %= p;
    update(1, 1, n, dfn[x], dfn[x] + sz[x] - 1, v);
}
ll tree_qe(ll x){
    return query(1 , 1, n, dfn[x], dfn[x] + sz[x] - 1);
}
int main(){
    MM(head, -1);
    RLL4(n, m, r, p);
    FOR(i, 1, n) RLL(a[i]);
    FOR(i, 1, n - 1){
        ll l, r; RLL2(l, r);
        add(l, r);
        add(r, l);
    }
    dfs1(r, 0);
    dfs2(r, r);
    build(1, 1, n);
    while(m --){
        ll s; RLL(s);
        if(s == 1){
            ll x, y, z;
            RLL3(x, y, z);
            up_lca_tree(x, y, z);
        }else if(s == 2){
            ll x, y;
            RLL2(x, y);
            PLN(qe_lca_tree(x, y) % p );
        }else if(s == 3){
            ll x, z;
            RLL2(x, z);
            tree_up(x, z);
        }else{
            ll x; RLL(x);
            PLN(tree_qe(x) % p);
        }
    }

    return 0;
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[[USACO08FEB]Hotel G (区间最大子段和变式,线段树)]]></title>
        <id>https://SandMonth.github.io/post/hotelGxds/</id>
        <link href="https://SandMonth.github.io/post/hotelGxds/">
        </link>
        <updated>2021-06-18T16:23:00.000Z</updated>
        <summary type="html"><![CDATA[<p>[USACO08FEB]Hotel G (区间最大子段和变式,线段树)</p>
]]></summary>
        <content type="html"><![CDATA[<p>[USACO08FEB]Hotel G (区间最大子段和变式,线段树)</p>
<!-- more -->
<h2 id="题意">题意:</h2>
<p>​		有n个房间(1-n),最开始所有的房间都是没有人住的,对你询问m次,每次有两个操作:</p>
<ul>
<li>1,查询房间,输入一个x找1-n的房间中有连续长度为x的空房,输出连续x个空房中的最左的房间号,并且房间设置为有人,如果没有连续的就输出0</li>
<li>2.退房,输入x,y代表房间号x -&gt; x + y - 1 退房, 即让房间为空</li>
</ul>
<h2 id="题解">题解:</h2>
<p>​	从题意上来看是一个区间的操作,所以可以往线段树方面去想.</p>
<p>​	使用线段树,那么考虑如何维护连续空房间的数量,对于连续的空房间其实就是类似找区间的最大子段和,我们能参考线段树区间最大子段和的维护值操作.所以也能确定线段树中的值存储每个点所代表区间的长度</p>
<blockquote>
<p>区间最大子段和 [解释]</p>
<p>给出n个数,q次操作,每次修改值或者查找区间[L,R]内最大的连续子段和</p>
</blockquote>
<p>​	对于树上的一个点使用 ls 和 rs 数组维护该点的左子树区间和右子树区间的最大连续字段和,使用tree数组维护ls和rs数组中的最大值加上左子树和右子树中的链接区间,所以建树过程为：</p>
<pre><code class="language-cpp">void build(ll rt, ll l, ll r){
    tree[rt] = ls[rt] = rs[rt] = len[rt] = r - l + 1;
    if(l == r) return;
    ll mid = (l + r) &gt;&gt; 1;
    build(rt &lt;&lt; 1, l, mid);
    build(rt &lt;&lt; 1 | 1, mid + 1, r);
}
</code></pre>
<p>​	对于题目中的查询操作,每次找该点到左树,左右树链接处和右树的最大连续字段和进行对比,如果左子树的连续子段大于则向左找,如果找了左子树没有找到则找左右子树链接部分,范围大于等于要找的话直接给出值，如果前面两个都没有找到则直接往右子树找</p>
<pre><code class="language-cpp">ll query(ll rt, ll l, ll r, ll v){
    push_up(rt);
    pt(rt);
    if(l == r) return l;
    ll mid = (l + r) &gt;&gt; 1;
    if(tree[rt &lt;&lt; 1] &gt;= v ) return query(rt &lt;&lt; 1, l, mid, v); //符合则向左子树继续找
    else if(ls[rt &lt;&lt; 1 | 1] + rs[rt &lt;&lt; 1] &gt;= v) return mid - rs[rt &lt;&lt; 1] + 1; //符合直接返回最左点
    else return query(rt &lt;&lt; 1 | 1, mid + 1, r, v); //上面都不符合则只可能在右子树中
}
</code></pre>
<p>​		题目中有两个修改操作,则对应的修改懒标记可以使用两个标签值来判断进行退房还是住房操作.(len 表示对应点的区间长度)</p>
<pre><code class="language-cpp">void push_up(ll x){ //懒标记
    if(lazy[x]){
        if(lazy[x] == 1){
            lazy[x &lt;&lt; 1] = lazy[x];
            lazy[x &lt;&lt; 1 | 1] = lazy[x];
            tree[x &lt;&lt; 1] = ls[x &lt;&lt; 1] = rs[x &lt;&lt; 1] = 0;
            tree[x &lt;&lt; 1 | 1] = ls[x &lt;&lt; 1 | 1] = rs[x &lt;&lt; 1 | 1] = 0;
            lazy[x] = 0;
        }else{
            lazy[x &lt;&lt; 1] = lazy[x &lt;&lt; 1| 1] = 2;
            tree[x &lt;&lt; 1] = ls[x &lt;&lt; 1] = rs[x &lt;&lt; 1] = len[x &lt;&lt; 1];
            tree[x &lt;&lt; 1 | 1] = ls[x &lt;&lt; 1 | 1] = rs[x &lt;&lt; 1 | 1] = len[x &lt;&lt; 1 | 1];
            lazy[x] = 0;
        }
    }
}
//修改
//tag =  1 清空 2添加 
void update(ll rt, ll l, ll r, ll L, ll R, ll tag){  
    push_up(rt);
    if(L &lt;= l &amp;&amp; r &lt;= R){
        if(tag == 1) tree[rt] = ls[rt] = rs[rt] = 0;
        else tree[rt] = ls[rt] = rs[rt] = len[rt];
        lazy[rt] = tag;
        return;
    }
    ll mid = (l + r) &gt;&gt; 1;
    if(L &lt;= mid) update(rt &lt;&lt; 1, l, mid, L, R, tag);
    if(mid  &lt; R) update(rt &lt;&lt; 1 | 1, mid + 1, r, L, R, tag);
    pt(rt);
}
</code></pre>
<p>对于修改完后每次维护的ls,rs,tree数组可以分组讨论</p>
<ul>
<li>ls： 有两种情况<br>
1. 该点的左子树房间全是空的,则  该点ls =  左子树区间长度 + 右子树的ls<br>
2. 该点的ls = 左子树的ls</li>
<li>rs: 有两种情况
<ol>
<li>该点的右子树房间全是空的，则该点rs = 右子树区间长度 + 左子树rs</li>
<li>该点的rs = 右子树的rs</li>
</ol>
</li>
<li>tree: 直接取左子树右子树和链接处最大即可，链接处为左子树的rs + 右子树的ls</li>
</ul>
<pre><code class="language-cpp">void pt(ll rt){
    if(tree[rt &lt;&lt; 1] == len[rt &lt;&lt; 1]){
        ls[rt] = ls[rt &lt;&lt; 1 | 1] + len[rt &lt;&lt; 1];
    }else ls[rt] = ls[rt &lt;&lt; 1];
    if(tree[rt &lt;&lt; 1 | 1] == len[rt &lt;&lt; 1 | 1]){
        rs[rt] = rs[rt &lt;&lt; 1] + len[rt &lt;&lt; 1 | 1];
    }else rs[rt] = rs[rt &lt;&lt; 1 | 1];
    tree[rt] = max(max(tree[rt &lt;&lt; 1], tree[rt &lt;&lt; 1 | 1]), ls[rt &lt;&lt; 1 | 1] + rs[rt &lt;&lt; 1]);
}
</code></pre>
<h2 id="完整代码">完整代码</h2>
<pre><code class="language-cpp">ll tree[N], ls[N], rs[N], len[N];
ll lazy[N];
ll n, m;
void build(ll rt, ll l, ll r){
    tree[rt] = ls[rt] = rs[rt] = len[rt] = r - l + 1;
    if(l == r) return;
    ll mid = (l + r) &gt;&gt; 1;
    build(rt &lt;&lt; 1, l, mid);
    build(rt &lt;&lt; 1 | 1, mid + 1, r);
}
void pt(ll rt){
    if(tree[rt &lt;&lt; 1] == len[rt &lt;&lt; 1]){
        ls[rt] = ls[rt &lt;&lt; 1 | 1] + len[rt &lt;&lt; 1];
    }else ls[rt] = ls[rt &lt;&lt; 1];
    if(tree[rt &lt;&lt; 1 | 1] == len[rt &lt;&lt; 1 | 1]){
        rs[rt] = rs[rt &lt;&lt; 1] + len[rt &lt;&lt; 1 | 1];
    }else rs[rt] = rs[rt &lt;&lt; 1 | 1];
    tree[rt] = max(max(tree[rt &lt;&lt; 1], tree[rt &lt;&lt; 1 | 1]), ls[rt &lt;&lt; 1 | 1] + rs[rt &lt;&lt; 1]);
}
void push_up(ll x){
    if(lazy[x]){
        if(lazy[x] == 1){
            lazy[x &lt;&lt; 1] = lazy[x];
            lazy[x &lt;&lt; 1 | 1] = lazy[x];
            tree[x &lt;&lt; 1] = ls[x &lt;&lt; 1] = rs[x &lt;&lt; 1] = 0;
            tree[x &lt;&lt; 1 | 1] = ls[x &lt;&lt; 1 | 1] = rs[x &lt;&lt; 1 | 1] = 0;
            lazy[x] = 0;
        }else{
            lazy[x &lt;&lt; 1] = lazy[x &lt;&lt; 1| 1] = 2;
            tree[x &lt;&lt; 1] = ls[x &lt;&lt; 1] = rs[x &lt;&lt; 1] = len[x &lt;&lt; 1];
            tree[x &lt;&lt; 1 | 1] = ls[x &lt;&lt; 1 | 1] = rs[x &lt;&lt; 1 | 1] = len[x &lt;&lt; 1 | 1];
            lazy[x] = 0;
        }
    }
}
//tag =  1 清空 2添加
void update(ll rt, ll l, ll r, ll L, ll R, ll tag){
    push_up(rt);
    if(L &lt;= l &amp;&amp; r &lt;= R){
        if(tag == 1) tree[rt] = ls[rt] = rs[rt] = 0;
        else tree[rt] = ls[rt] = rs[rt] = len[rt];
        lazy[rt] = tag;
        return;
    }
    ll mid = (l + r) &gt;&gt; 1;
    if(L &lt;= mid) update(rt &lt;&lt; 1, l, mid, L, R, tag);
    if(mid  &lt; R) update(rt &lt;&lt; 1 | 1, mid + 1, r, L, R, tag);
    pt(rt);
}

ll query(ll rt, ll l, ll r, ll v){
    push_up(rt);
    pt(rt);
    if(l == r) return l;
    ll mid = (l + r) &gt;&gt; 1;
    if(tree[rt &lt;&lt; 1] &gt;= v ) return query(rt &lt;&lt; 1, l, mid, v);
    else if(ls[rt &lt;&lt; 1 | 1] + rs[rt &lt;&lt; 1] &gt;= v) return mid - rs[rt &lt;&lt; 1] + 1;
    else return query(rt &lt;&lt; 1 | 1, mid + 1, r, v);
}
int main(){
    RLL2(n, m);
    build(1, 1, n);
    while(m --){
        ll s; RLL(s);
        if(s == 1){
            ll k; RLL(k);
            if(tree[1] &gt;= k){
                ll ans = query(1, 1, n, k);
                PLN(ans);
                update(1, 1, n, ans, ans + k - 1, 1);
            }else PLN((ll)0);
        }else{
            ll l, r; RLL2(l, r);
            update(1, 1, n, l, l + r - 1, 2);
        }
    }
    return 0;
}
</code></pre>
]]></content>
    </entry>
</feed>